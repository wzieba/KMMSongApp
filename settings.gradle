pluginManagement {
    apply from: "dependencies.gradle"

    plugins {
        id 'org.jetbrains.kotlin.plugin.serialization' version "${versions.kotlin}"
    }
}

include ":androidApp"
include ":domain"
include ":local"
